{
	"name": "frontend",
	"version": "0.0.0-semantically-released",
	"private": true,
	"scripts": {
		"dev": "next dev --turbopack & docker compose -f ../../.config/docker/docker-compose.yml up",
		"build": "next build --turbopack",
		"start": "next start",
		"lint": "biome check",
		"format": "biome format --write",
		"test:unit": "vitest --run --exclude='**/*.spec.{ts,tsx}'",
		"test:integration": "vitest --run --config ./vitest.integration.config.mts",
		"test": "vitest --run"
	},
	"dependencies": {
		"next": "15.5.0",
		"react": "19.1.0",
		"react-dom": "19.1.0"
	},
	"devDependencies": {
		"@playwright/test": "1.55.0",
		"@tailwindcss/postcss": "^4",
		"@types/node": "^20",
		"@types/react": "^19",
		"@types/react-dom": "^19",
		"tailwindcss": "^4",
		"typescript": "^5"
	}
}
